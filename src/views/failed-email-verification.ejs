<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Email Verification Failed</title>
    <style>
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      body {
        font-family: "Arial", sans-serif;
        background-color: #f0f4f8;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        color: #333;
      }

      .container {
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 20px;
      }

      .verification-card {
        background-color: #ffffff;
        border-radius: 12px;
        box-shadow: 0 8px 30px rgba(0, 0, 0, 0.1);
        padding: 40px;
        text-align: center;
        width: 350px;
        transition: transform 0.3s;
      }

      .verification-card:hover {
        transform: translateY(-5px);
      }

      h1 {
        color: #d20735; /* Red color for failure */
        margin-bottom: 20px;
        font-size: 24px;
        font-weight: bold;
      }

      p {
        color: #555;
        margin-bottom: 30px;
        font-size: 16px;
        line-height: 1.5;
      }

      .btn {
        display: inline-block;
        background-color: #d20735; /* Red color for failure */
        color: white;
        padding: 12px 25px;
        text-decoration: none;
        border-radius: 5px;
        font-size: 16px;
        transition: background-color 0.3s, transform 0.3s;
        box-shadow: 0 4px 15px rgba(210, 7, 53, 0.2);
      }

      .btn:hover {
        background-color: #a2062b; /* Darker red on hover */
        transform: translateY(-2px);
      }

      .icon {
        font-size: 50px;
        color: #d20735; /* Red color for failure */
        margin-bottom: 20px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="verification-card">
        <div class="icon">❌</div>
        <h1>Email Verification Failed</h1>
        <p style="color: #d20735; text-transform: uppercase"><%= message %></p>
        <p>
          Unfortunately, we could not verify your email address. Please check
          the link you received or try again.
        </p>
        <!-- <a href="resend_verification.html" class="btn"
          >Resend Verification Email</a
        > -->
      </div>
    </div>
  </body>
</html>
